<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hcservices">

    <application
        android:allowBackup="true"
        android:icon="@drawable/pic8"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".list"></activity>
        <activity android:name=".EditDetails" />
        <activity
            android:name=".servicehome"
            android:label="@string/title_activity_servicehome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".serviceprovider"
            android:label="@string/title_activity_serviceprovider"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Plumber" />
        <activity android:name=".carpenter" />
        <activity android:name=".electric" />
        <activity android:name=".ac" />
        <activity android:name=".Singup" />
        <activity android:name=".signinactvity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>